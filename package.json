{
  "name": "fry-calculator",
  "description": "Calculator for cost of braces",
  "version": "1.0.0",
  "author": "Casey Hill <bignamehere@gmail.com>",
  "dependencies": {
    "gatsby": "^1.9.158",
    "gatsby-link": "^1.6.34",
    "gatsby-plugin-react-helmet": "^2.0.3",
    "react-helmet": "^5.2.0"
  },
  "keywords": [
    "Calculator"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write \"src/**/*.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "axios": "^0.17.1",
    "gh-pages": "^1.1.0",
    "prettier": "^1.10.2",
    "svg-knob": "^1.4.2"
  }
}
